extends ../layouts/layout

block vars
  - var isArabic = true
  - var langSwitchBase = 'contact'

block head
  title اتصل بنا | ترافلا

block pageHeaderData(pageData)
  - pageData.title = contactPageTitle
  - pageData.breadcrumbs = [{ label: contactBreadcrumbHome, url: 'index.html' }, { label: contactBreadcrumbContact, url: null }]

block content
  include ../partials/contact-content
