extends ../layouts/layout

block vars
  - var isArabic = false
  - var langSwitchBase = 'pricing'

block head
  title Pricing | Trufla

block pageHeaderData(pageData)
  - pageData.title = isArabic ? 'الأسعار' : 'Pricing'
  - pageData.breadcrumbs = isArabic ? [{ label: 'الرئيسية', url: 'index.html' }, { label: 'الأسعار', url: null }] : [{ label: 'Home', url: 'index.html' }, { label: 'Pricing', url: null }]

block content
  include ../data/pricing-data
  include ../partials/pricing-content
