include variables

header(class="bg-white dark:bg-gray-950/90 border-b border-gray-200 dark:border-gray-800 sticky top-0 z-50 relative")
  nav(aria-label=navAriaLabel class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8")
    div(class="flex items-center justify-between gap-4 py-3 md:py-4")
      //- Logo
      a(href=homeUrl class="flex-shrink-0 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded")
        img(src=logo.src alt=logo.alt width="200" height="80" class="h-14 md:h-16 dark:invert")

      //- Mobile menu toggle (visible on small screens only)
      button#mobile-menu-toggle(type="button" aria-label=mobileMenuAriaLabel aria-expanded="false" aria-controls="mobile-menu" data-open-label=mobileMenuAriaLabel data-close-label=mobileMenuCloseAriaLabel class="md:hidden flex items-center justify-center w-10 h-10 rounded-lg text-secondary dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 cursor-pointer")
        span(class="fa fa-bars text-lg mobile-menu-icon-open" aria-hidden="true")
        span(class="fa fa-times text-lg mobile-menu-icon-close hidden" aria-hidden="true")

      //- Navigation links + actions (desktop)
      ul(class="flex items-center gap-x-2 md:gap-x-4 lg:gap-x-6 list-none m-0 p-0")
        //- Home (icon + text)
        li(class="hidden md:block")
          a(href=homeUrl class="flex items-center gap-1.5 text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary font-semibold text-base focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded px-1 py-2")
            span(class="fa fa-home" aria-hidden="true")
            span= navHomeLabel

        //- Page links
        each link in navLinks
          li(class="hidden md:block")
            a(href=link.href class="text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary font-semibold text-base focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded px-1 py-2")= link.label

        //- Language switcher (when langSwitchBase is set, e.g. on login / login-ar)
        if langSwitchBase
          li
            a(href=langSwitchBase + (isArabic ? '.html' : '-ar.html') class="flex items-center gap-1.5 text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary font-semibold text-sm focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 rounded px-2 py-2 transition-colors" title=isArabic ? 'English' : 'العربية')
              span(aria-hidden="true")= isArabic ? '🇬🇧' : '🇸🇦'
              span= isArabic ? 'English' : 'العربية'
        //- Dark / Light toggle
        li
          button#theme-toggle(type="button" aria-label=themeToggleAriaLabel class="flex items-center justify-center w-10 h-10 rounded-lg text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary hover:bg-gray-100 dark:hover:bg-gray-800 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 cursor-pointer text-lg")
            //- Sun icon (visible in dark mode)
            span(class="fa fa-sun-o hidden dark:inline" aria-hidden="true")
            //- Moon icon (visible in light mode)
            span(class="fa fa-moon-o inline dark:hidden" aria-hidden="true")

        //- Get a Quote CTA
        li
          a(href=navCtaHref class="inline-flex items-center justify-center bg-primary text-white hover:bg-secondary border border-primary hover:border-secondary rounded font-semibold text-sm px-4 py-2.5 md:px-6 md:py-2.5 h-10 focus:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2 transition-colors")= navCtaLabel

    //- Mobile menu panel (slide-down, visible when toggled)
    div#mobile-menu(class="hidden md:hidden absolute left-0 right-0 top-full bg-white dark:bg-gray-950 border-b border-gray-200 dark:border-gray-800 shadow-lg z-40" role="dialog" aria-label=navAriaLabel)
      div(class="max-w-7xl mx-auto px-4 py-4 flex flex-col gap-2")
        a(href=homeUrl class="flex items-center gap-2 py-3 px-2 text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary font-semibold rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800")
          span(class="fa fa-home" aria-hidden="true")
          span= navHomeLabel
        each link in navLinks
          a(href=link.href class="py-3 px-2 text-secondary hover:text-primary dark:text-gray-200 dark:hover:text-primary font-semibold rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800")= link.label
        a(href=navCtaHref class="py-3 px-2 text-center bg-primary text-white hover:bg-secondary font-semibold rounded-lg")= navCtaLabel
