meta(charset='utf-8')
link(rel="preconnect" href="https://fonts.googleapis.com")
link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
link(rel="preconnect" href="https://images.unsplash.com")
//- Apply dark class before paint to prevent flash of wrong theme
script.
  (function(){try{if(localStorage.getItem('theme')==='dark'||(!localStorage.getItem('theme')&&window.matchMedia('(prefers-color-scheme: dark)').matches)){document.documentElement.classList.add('dark')}}catch(e){}})();
meta(name="description" content=pageDescription || 'Trufla delivers insurance templates and claims support for your business.')
meta(name="keywords" content=pageKeywords || 'insurance, templates, claims, payments, Trufla')
// Meta - Author
meta( name="author" content="Mamdouh Ramadan")
// Meta - Theme Color
meta(name="theme-color" content="#42415b")
// Meta - Viewport
meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
// Google Font - Poppins, Roboto, Almarai (Arabic)
link(rel='stylesheet' href='https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Roboto:wght@300;400;700&family=Almarai:wght@300;400;700;800&display=swap')
// Font Awesome 4.7 from CDN (icons only; no local font files)
link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css' crossorigin='anonymous')
// Main Libraries CSS (normalize only; Font Awesome loaded above)
link(rel='stylesheet' href='css/libs.css')
// Main Style CSS
link(rel='stylesheet' href='css/main.css')
// Tailwind CSS (load after main so utilities can override if needed)
link(rel='stylesheet' href='css/tailwind.css')

// Adding FavIcon
link(href="favicon.ico" rel="icon" type="image/x-icon")
// PWA Manifest File
link(rel="manifest" href="./manifest.json")

link(rel="apple-touch-icon" href="images/android-chrome-192x192.png")

link(rel="canonical" href=canonicalUrl)

meta(property="og:type" content="website")
meta(property="og:title" content=pageTitle || 'Trufla')
meta(property="og:description" content=pageDescription || 'Insurance templates and claims support.')
meta(property="og:url" content=canonicalUrl)
meta(property="og:image" content=ogImageUrl)
meta(name="twitter:card" content="summary_large_image")
meta(name="twitter:title" content=pageTitle || 'Trufla')
meta(name="twitter:description" content=pageDescription || 'Insurance templates and claims support.')
meta(name="twitter:image" content=ogImageUrl)
